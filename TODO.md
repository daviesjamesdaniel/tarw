# TODO

Feature roadmap, in priority order:

1. Conversation/thread view — group related messages instead of a flat list.
2. Signatures in the composer.
3. Filters/rules (auto-move by sender, etc.).
4. Body/content search — search bar currently only matches headers (subject/from/to/cc) and operators like `is:unread`/`has:attachment`, not message bodies.
