# Copy this file to .env and fill in real values yourself (never commit .env).
#
# Optional: auto-registers this Gmail address on first run if no accounts
# exist yet. Not needed otherwise - just use "Add account" in the app.
IMAP_USER=
